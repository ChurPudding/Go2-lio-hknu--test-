# 실외 실측 (2026-07-31 오전, 운동장)

## bag 목록

| bag | 길이 | cloud | 방식 | 용도 |
|---|---|---|---|---|
| `go2_outdoor_0731_1114` | 306 s | 4,711 | 명시 | **주행 한 바퀴** |
| `go2_outdoor_0731_1119` | 23 s | 352 | 명시 | **제자리 2회전** |
| `go2_outdoor_all_0731_1128` | 287 s | 4,365 | `-a` + `-x` | 전체 토픽본 |
| `go2_outdoor_all_0731_1122` | 3 s | 0 | `-a` | 실패 (발견 오류) |
| `go2_outdoor_all_0731_1125` | 8 s | 125 | `-a` + `-x` | 시험 |

레이트 전부 정상 (cloud 15 Hz, lowstate 500 Hz, imu 250 Hz, gnss 1 Hz).
어제와 달리 연결이 끝까지 유지됨. 인터페이스 `enx00e04c360e79` (USB-이더넷).

## `-a` 실패 원인 확정

`Failure in topics discovery` 의 범인은 두 메시지 타입이었다.
노트북의 `unitree_go` 패키지에 없어 순회가 중단되고, 뒤쪽 토픽은 구독조차 못 받는다.

- `unitree_go/ConfigChangeStatus`  (`/config_change_status`)
- `unitree_go/VoxelMapCompressed`  (`/utlidar/voxel_map_compressed`)

```bash
ros2 bag record -a \
  -x '/config_change_status|/utlidar/voxel_map_compressed|/arm_Command|/qt_.*' \
  -o go2_outdoor_all_$(date +%m%d_%H%M)
```

이렇게 하면 `-a` 도 정상 동작한다 (`all_0731_1128` 로 확인).

## 8번 검증 — 제자리 2회전 (`0731_1119`)

로봇 기준값: 누적 yaw **+742.4 deg**, 이동 1.79 m, 시작-끝 0.40 m, 반경 25 cm.

Point-LIO 결과 (`tools/spin_check.py`):

| 항목 | 값 | 기대 | 판정 |
|---|---|---|---|
| 회전축 | (+0.106, -0.165, -0.981) | (+0.169, -0.194, -0.966) | **4.1 deg — R_LB 검증됨** |
| 연직축 누적 회전 | +505.0 deg | +742.4 | 68% — **퇴화** |
| 총 회전량 | +727.9 deg | +742.4 | 2% — 회전 자체는 감지 |
| 원 반지름 | 0.355 m | 0.322 (LEVER) | 3.3 cm 차, 잔차 RMS 0.103 m |

**축은 맞는데 연직 성분만 68%** → 회전은 봤으나 자세가 흔들렸다.
구간별 각속도가 -4 ~ +113 deg/s 로 요동 (실제는 약 32 deg/s 일정해야 함).

## 실외 주행 LIO (`0731_1114`, Point-LIO, -r 0.5)

| | GPS | Point-LIO |
|---|---|---|
| 총거리 | 342.1 m | **28,038.8 m** |
| 시작-끝 | 7.1 m | 26,691.3 m |

수평오차 RMS 8,248 m / 최대 26,695 m. 최적 yaw 정렬(-97.5 deg) 후의 값이다.

| 구간 | 평균 오차 |
|---|---|
| 0~60 s | 16.5 m |
| 60~120 s | 85.4 m |
| 120~180 s | 125.2 m |
| **180~240 s** | **1,872 m  <- 발산** |
| 240~300 s | 15,576 m |

**발산 전 구간도 못 쓴다.** 180초에 이미 125 m.
180초까지만 잘라 본 결과: RMS 89.6 m, 최대 142.5 m (t=155 s).
궤적 **모양 자체가 다르다** — 단일 변환으로 정렬되지 않는다.

## 원인 — 관측성 부족 (교정으로 해결 불가)

점군 자체는 정상이다. 오히려 실내보다 사거리가 길다.

| | 스캔당 점 | 95% 사거리 |
|---|---|---|
| 실내 `go2_run_full` | 4,168 ± 17 | 1.81 m |
| 실외 `0731_1119` | 3,989 ± 75 | 3.02 m |

문제는 **점의 분포**다.

| 스캔 | 총 점 | 지면 비율 | 지면외 |
|---|---|---|---|
| 0 | 4,041 | 95.7% | 173 |
| 100 | 3,868 | 97.8% | 84 |
| 200 | 3,998 | **98.5%** | **61** |

**96~99% 가 지면.** 평면 하나로는 요와 수평 위치를 제약할 수 없다.
실내에서는 벽이 간헐적으로나마 잡아줬으나 운동장에는 없다.

교정(외부 파라미터·축척)으로는 해결되지 않는다. 최적 정렬 후에도 잔차 89.6 m 이고,
오차가 t=25 s(이동 시작)부터 단조 증가하는 적분 표류이기 때문이다.
교정은 발산 시각을 늦출 뿐이다.

## 결론 — 대회 전략

- **GPS 가 주 위치 소스여야 한다.** 실외 HDOP 0.78, 정확도 약 2.4 m
- **LIO 는 국소 장애물 회피와 단기 자세 추정으로 역할 축소**
- 17번(이중 EKF)은 선택이 아니라 **필수**
- 16번(실외 재검증)의 "퇴화 위험" 은 예상보다 심각하게 실현됨
